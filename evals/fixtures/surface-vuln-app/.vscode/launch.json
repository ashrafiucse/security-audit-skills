{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch API",
      // SEC-10: live token in a debug launch config (shared + committed)
      "env": {
        "GITHUB_TOKEN": "ghp_FakeDoNotUseExample0000000000000000000000"
      }
    }
  ]
}
