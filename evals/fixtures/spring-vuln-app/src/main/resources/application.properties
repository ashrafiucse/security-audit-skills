# Fixture: intentionally insecure Spring Boot config. FAKE values.
spring.datasource.url=jdbc:postgresql://db.internal.example.com/app
spring.datasource.password=spring-prod-pass-2026

# SEC-02: every actuator endpoint exposed (env + heapdump leak secrets)
management.endpoints.web.exposure.include=*

# SEC-03: full stacktraces to clients
server.error.include-stacktrace=always

# SEC-04: session cookie without Secure flag
server.servlet.session.cookie.secure=false
