# MISSES — the miss ledger

Every finding the skills failed to catch, logged the same session it was
discovered — then converted into a pattern + fixture (see MAINTENANCE.md §3).
This file is the memory that makes the same miss impossible twice. Rows are
never deleted; closed rows stay as history.

## Open misses

(none)

## Closed misses

| Date | Source | What was missed | Skill | Pattern added | Fixture |
|---|---|---|---|---|---|
| 2026-09-22 | coverage audit | 9 uncovered classes: prototype pollution, NoSQL operator injection, XXE, ReDoS, open redirect, trusted-header authz, TOCTOU races, WebSocket/SSE authz, generic file-upload abuse | injection-flaws, auth-review | sections in both SKILL.md + patterns.md | node-vuln-app-2, python-vuln-app-2 |
| 2026-09-22 | coverage audit | route census (unguarded endpoints found only by pattern luck), second-order/stored flows, query-builder raw sinks, multi-line query construction | auth-review, injection-flaws | census table in auth-review §1; second-order + builder + multiline sections | node-vuln-app-3 |
| 2026-09-22 | coverage audit | no fix-verification loop (audits were one-shot), no automated eval scoring, missing evals/run.md | security-audit, evals | re-audit mode in security-audit; scripts/score_audit.py + sample; evals/run.md | sample-audit-node2.md |
| 2026-09-22 | coverage audit | generic mass assignment (only Laravel/Django/Rails had it), postMessage handlers, SSRF egress review (NetworkPolicy/IMDSv2), supply-chain hygiene (dependency confusion/typosquats) | auth-review, config-hardening, injection-flaws, container-iac-security, dependency-vulns | mass-assignment greps; §7 client-side section; egress/IMDSv2 checks; Step 2.5 supply chain | node-vuln-app-3 (SEC-06), spa-vuln-app, iac-vuln-app (2d/4), supply-chain-vuln |
| 2026-09-22 | research round | no LLM/AI-stack coverage; `hf_` tokens planted but unmatchable (secrets recall gap); JWT algorithm confusion / JWKS kid injection / PKCE uncovered; CI `${{ github.event.* }}` script injection, CORS null origin, cache deception, ingress-nginx snippets uncovered | new skill llm-security; secrets-detection; auth-review; config-hardening; container-iac-security | llm-security SKILL.md; hf_/sk-ant-api03_/gsk_ patterns; JWT confusion+kid+PKCE bullets; CI-injection grep+env: fix pattern; ingress annotation checks | llm-vuln-app, python-vuln-app (hf_ now asserted in CI), iac-vuln-app 3f |
| 2026-09-22 | research round | knowledge base lacked 2024–2025 landmark CVEs with in-repo detection | cve-research | vuln-db entries: CVE-2024-3094 (xz backdoor), CVE-2025-29927 (Next.js middleware bypass), CVE-2024-6387 (regreSSHion) | n/a (version-check entries) |
| 2026-09-22 | deep research round | knowledge base thin on library-family RCEs and OS/runtime landmarks across ecosystems | cve-research, injection-flaws, rails-security | 16 entries: jsonwebtoken family, qs + lodash pollution, semver ReDoS, PyYAML FullLoader, urllib3/requests cred leaks, Text4Shell, fastjson autoType, Shiro550, Rails render-file, php-cgi Windows RCE, PwnKit, Grafana traversal, GitLab reset takeover, sudo Baron Samedit | java-libs-vuln, rails-libs-vuln (sinks wired into frameworks.md + rails-security) |
| 2026-09-22 | e2e baseline round | first real agent-run audits exposed: scorer couldn't match global/absence findings (bare-file citations); node2/llm fixtures lacked ground truth for real extras the agents found (no lockfile, no headers, no auth layer, unpinned deps) | score_audit, fixtures | `file:-` file-level anchors + bare-file support on BOTH recall and precision sides; ground truth extended with verified extras | node-vuln-app-2 (+3 rows), llm-vuln-app (+1 row); reports archived under evals/rounds/2026-09-22-e2e-baseline/ |
| 2026-09-22 | user report (session) | **route-only audits are structurally blind to flow-level issues** — order/payment/invoice individually clean, compromise appears only across the sequence (user-reported exact scenario) | new skill flow-security | 8 flow classes F1–F8: unguarded transitions, cross-step data provenance, association IDOR, replay at terminal steps, step-skipping, post-boundary mutation, amount drift, privilege transitions; flow reconstruction + invariant-table method; WSTG-BUSL/API mapping | flow-vuln-app (8 planted, individually-clean endpoints) |
| 2026-09-22 | user report (session) | **main code clean, risk lives in packages** — advisory scanners are silent on discontinued/unfixable libs, compromised releases without CVEs, safe-package-dangerous-usage, and vendored copies invisible to manifest scanners (user: "I found the same issue in our system") | dependency-vulns | Step 1 reachability procedure (present→used→dormant); Step 2.7 beyond-CVEs; references/dangerous-packages.md (vm2, request, event-stream 3.3.6, ua-parser-js 0.7.29/0.8.1/1.0.12, eslint-scope 3.7.2, coa/rc, node-ipc 11.x, cross-env 7.0.x, dangerous-usage table, vendored banner thresholds) | dep-risk-vuln-app (6 planted: vm2+reachability, lodash.merge usage, request, compromised version, vendored jQuery 1.8.3, no lockfile) |
| 2026-09-22 | proactive gap research | four uncovered classes found by blind-spot analysis: multi-tenant scoping (one missing tenant filter = whole-tenant bleed), deferred/stored SSRF via registered callbacks, archive extraction attacks (zip-slip + symlink members — only bombs were covered), unicode/identifier comparison hygiene | auth-review, injection-flaws | tenant-scope census (SCOPED/UNSCOPED/GLOBAL audit + request-body tenant switch + cache keys); deferred-SSRF registration+fetch-time checks; extraction entry-containment+symlink rules; NFC/NFKC + punycode + fullwidth-escape comparison rules | gaps3-vuln-app (5 planted: unscoped reports read, hook registration, delivery fetch, extractAllTo overwrite, host.includes allowlist) |
| 2026-09-22 | e2e gap-hunt wave | 9/12 fixtures audited at 100/100/0 after conversions (+9 agent-verified real extras: healthchecks, no-auth censuses, manifest gaps, below-fix-line express, dormant-deps verdicts); 3 runs (node-3, crypto, dep-manifests) BLOCKED by subagent model availability — open for next round | fixtures | ground truths extended; absence-anchor convention added (anchor to the absent file, e.g. requirements.txt:-) | reports archived under evals/rounds/2026-09-22-e2e-gap-hunt/; scoreboard rows labeled with the blockage |
| 2026-09-22 | user request: incident research | knowledge base lacked mass-exploited/incident CVEs and breach-derived lessons (Metasploit-class waves, Equifax/Capital One/SolarWinds-class breaches) | cve-research | 8 vuln-db entries (44 total): Jenkins CVE-2024-23897, Confluence CVE-2023-22515, Spring Cloud Gateway CVE-2022-22947, PHPUnit eval-stdin CVE-2017-9841, lodash.template CVE-2021-23337, ThinkPHP CVE-2018-20062, Exim CVE-2019-10149, Apache httpd CVE-2021-41773 — all Metasploit/KEV-context with in-repo detection; references/notable-incidents.md maps 15 real incidents → the exact repo checks that catch them | n/a (version/file-detection entries; incidents file is reference knowledge) |
| 2026-09-22 | enrichment round | **C/C++ native code fully uncovered** (the only major language family with zero patterns); AWS IAM escalation paths only wildcard-checked; API-key auth unreviewed; 3 modern secret providers unmatchable; GraphQL aliasing DoS missing | injection-flaws, container-iac-security, auth-review, secrets-detection, graphql-security | C/C++ native pack (format strings, strcpy/gets, system(), overflow-to-alloc, TOCTOU + memory-safe idioms); references/aws-iam-escalation.md (PassRole+compute, self-modification, broken trust, ExternalId); API-key auth section (URL keys, scoping, rotation, timing, generation); sntrys_/nfp_/sk-or- patterns; aliasing amplification row; +3 vuln-db entries (47 total: Drupalgeddon2, PHP-FPM/nginx, node-serialize no-fix) | c-vuln-app (5 planted + safe_service.c) |
| 2026-09-22 | user request: self-sufficiency | no repeatable way to BUILD custom skills (no scaffold, no codified authoring method) — and no domain skill for the user's own platform class (course/e-learning, persona-driven) | new skills skill-forge + course-platform-security | skill-forge: scoping from personas/pains, scaffold script (skills/skill-forge/scripts/new_skill.sh), 8 authoring laws from LEARNINGS, blind-test protocol, wiring checklist; course-platform-security: gating truth, preview-vs-full leaks, enrollment state machine, cohort scoping, admin surfaces — public/student/admin personas | course-vuln-app (5 planted: catalog unfiltered, full-content preview, self-enrollment, cohort query-scope, admin unguarded) |
| 2026-09-22 | user request: more sources | knowledge intake limited to OSV/KEV/NVD/EOL — no collector for in-the-wild 0-day data or public-exploit (weaponization) status | cve-research, workflows | watch-0days.yml (weekly Mon 06:30): Project Zero 0days-in-the-wild tracker — per-CVE md files under 0day-RCAs/<year>/, commits-window diffing; watch-metasploit.yml (weekly Mon 06:45): new CVE-backed exploit modules via commits on modules/exploits + per-commit file extraction, with a fail-loudly path guard; Step 3 source sections + notable-incidents live-sources footer; CI now validates workflow YAML | n/a (issue-digest collectors; vuln-db entries land via triage) |
| 2026-09-22 | e2e frameworks round | mobile audit missed exported `SyncService` — guidance existed as a table row but no per-TYPE census (agents eyeball activities/providers, skip services/receivers) | mobile-security | component census BY TYPE (four separate greps: activity/service/receiver/provider) | mobile-vuln-app (existing plant, line ref corrected :31) |
| 2026-09-22 | e2e frameworks round | generic secrets regex structurally cannot match unquoted `.properties` creds (`spring.datasource.password=...`) — only the framework skill caught it | secrets-detection | anchored unquoted key=value pattern (keyword anywhere in key, EOL-anchored); FP-swept repo-wide | spring-vuln-app (CI assertion added) |
| 2026-09-22 | e2e frameworks round | scorer dropped dotfile citations entirely (`.env:3` unparseable — laravel `.env` rows silently excluded from scoring); rails `send_file` "miss" was actually ground-truth line drift (:21 vs :25) causing false miss attribution | score_audit, rails fixture | FILE_RE now allows dotfiles + extension-less manifests (Gemfile); rails/spring Where columns re-anchored to actual lines | laravel-vuln-app (rows 1/1b split), rails-vuln-app (rewritten) |
| 2026-09-22 | e2e frameworks round | agents found 12 real findings fixtures didn't plant (EOL versions, no-lockfiles, over-returning, inert SecurityConfig) — every scorer "phantom" was a ground-truth gap, zero fabrications across 6 reports | fixtures | ground truths extended with verified extras (file-level `:-` anchors for absence findings) | django +2, graphql +2, laravel +2, rails +2, spring +2 rows |

## How to add a row

1. When a user reports a miss (issue) or a quarterly benchmark (Juice Shop
   etc.) finds one, open a row **the same session** — status open.
2. Write the fixture reproducing it (`evals/fixtures/…` + expected findings).
3. Add the detection guidance to the skill.
4. Re-run `python3 scripts/selftest_patterns.py` + `python3 scripts/validate.py`.
5. Move the row to Closed with pattern + fixture links.
